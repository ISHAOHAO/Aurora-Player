# 2026-08-12

## Aurora Player（播发器项目）
- 用户提供完整 PRD（Project Aurora：Win11 沉浸式全能视频播放器 + DLNA MediaRenderer + 媒体中心，参考 Mineradio 视觉语言）。
- 按 PRD 结尾建议，在 `docs/` 下产出 4 份工程文档：
  1. `01-UI-UX设计规范.md`（设计令牌/Glass 三级材质/页面规范/组件/动效/错误态）
  2. `02-音视频引擎技术设计.md`（FFmpeg+libass+libplacebo+D3D11 架构、解码降级链、HDR/色彩、音频、字幕、缓存、状态机、IPC schema、进程隔离）
  3. `03-DLNA-UPnP技术规格.md`（SSDP、Device/Service Description、AVTransport/RenderingControl/ConnectionManager、Eventing、HTTP 拉流、防火墙、真机兼容策略）
  4. `04-测试与兼容性矩阵.md`（素材命名规范、用例模板、GPU/显示器/网络矩阵、DLNA 真机矩阵、稳定性/性能/安全验收门槛）
- 关键选型：UI = React+TS+WebView2，核心 = C++ 原生引擎，多进程（UI/engine/dlna/thumb）。
- 下一步候选：项目脚手架搭建、开发排期、UI 原型。

## 开发决策（/grill-me 三轮拷问锁定，用户逐条确认）
- **范围**：MVP 为硬里程碑 → 持续加深至 P1（1B）
- **技术栈**：Electron + mpv 子进程 `--wid` 原生窗口嵌入（保 D3D11/HDR 零拷贝链路）；React UI；引擎接口抽象，未来可换自研核心（2A/7A/13A）
- **DLNA**：独立 Node 进程 aurora-dlna；SSDP/SOAP 协议栈全部自研，不用年久失修的库；自研虚拟 DMC 测试脚本做验收（3A/8A/14A）
- **HDR**：完整决策链代码实现，真机画质由用户终验（4A）
- **节奏**：里程碑必交付 + 方向性岔路口才提问；实现受限项自行降级并记入《实现偏差清单》（5A+C/15A）
- **许可**：mpv 官方/社区构建（GPL），进程隔离 + 附许可声明（6A）
- **UI**：先做首页+播放页视觉原型（HTML 静态）确认风格再实现（10B/16A）
- **分发**：MVP 便携包；NSIS 安装包放 RC 阶段（11A+B）
- **依赖**：允许下载 mpv(shinchiro)/ffmpeg(gyan.dev)/Blender 开源测试片（12A）
- 环境：本机无 MSVC/ffmpeg/HDR 显示器；有 Node 22/24、Python 3.13、Git

## M0 视觉原型（风格已定稿）
- `prototype/aurora.css`：设计令牌 + Glass 三级材质 + 双主题(dark/light, data-theme) + 环境光舞台 `.ambient-stage`（Mineradio 式柔光场）
- `prototype/home.html`：首页原型 + 主题切换按钮（localStorage 持久化）
- `prototype/player.html`：播放页原型（恒暗）
- 风格迭代：去 AI 味（禁发光/锥形渐变/玻璃滥用/pill堆砌，规范 v1.1）→ Mineradio 环境光 + 双主题（规范 v1.2）
- 里程碑计划：M0 原型 → M1 播放骨架（Electron+mpv --wid）→ M2 完整 UI → M3 播放能力 → M4 DLNA → M5 HDR/打磨 → M6+ P1
- 用户确认计划后动工（16:27"确认"）

## M1 播放骨架（已完成并验证 17:45）
- `app/`：Electron v43.4.0 + mpv v0.41.0（shinchiro 20260811，存 `runtime/mpv/`）
- 链路验证通过：HWND 嵌入、命名管道 `\\.\pipe\aurora-mpv` IPC、原生菜单控制、标题栏状态轮询
- M1 控制 UI 用 mpv OSC + 原生菜单；自绘 UI 留 M2（透明叠加子窗口方案，mpv 子窗口 Z 序在 HTML 之上）
- 偏差记录：`docs/05-实现偏差清单.md`（D1 React 延后 M2、D2 自绘控制层延后 M2 等）
- 环境坑：沙箱 curl 不能写中文绝对路径/区外路径；7z 用 `C:\Windows\System32\tar.exe` 解压

## M2 完整 UI（已完成并验证 18:12）
- 架构：三窗口 — homeWin(React 首页) / videoWin(mpv --wid) / overlayWin(透明叠加控制层, owned+alwaysOnTop, 空闲 setIgnoreMouseEvents(true,{forward}))
- 渲染层：Vite+React19+TS strict, `app/renderer`, `npm run build` → dist, hash 路由 #/home #/player 共用 bundle
- 功能：打开文件(Ctrl+O/磁贴/Hero)、最近播放持久化+续播位置、控制层自动隐藏/唤醒、键盘转发(空格/方向/M/F)、全屏(F)、双主题首页
- 验证：lavfi 生成 10s 测试片播放走通、recent.json 落盘、tsc 零错误
- 新增偏差 D6-D10(控制台/鼠标交互/缩略图气泡/海报刮削/设置页 → M3+)
- 下一步 M3 播放能力：控制台抽屉、input.conf 键鼠、缩略图进程、音轨/字幕轨菜单、网络媒体

## M3 播放能力（已完成并验证 18:30）
- 输入模型转向：叠加层常驻交互,React 统一处理单击暂停/双击全屏/滚轮音量/右键菜单;**放弃 input.conf 方案**(mpv 窗口不再接收输入)——双击全屏需控制 Electron 窗口,单一输入源更可靠
- 控制台抽屉(360px glass-2):基础(速度/比例/A-B循环)+音频(音轨/延迟)+字幕(轨/延迟/字号);视频/高级占位
- file-loaded 事件 → track-list/chapter-list → 轨道菜单(字幕可关) + 章节标记点 + 悬停时间气泡
- 首页网络媒体磁贴启用(URL 弹窗串流播放)
- 真实缩略图帧降级 P1(D13);设置页 D10 → M5;外部字幕/双语 D12 → M5
- 下一步 M4 DLNA:独立进程 aurora-dlna,SSDP/SOAP 自研,虚拟 DMC 测试脚本验收

## 黑屏事故与架构修正（19:10，用户上报有声无画）
- 根因:Chromium DComp 合成层盖住 mpv --wid 子窗口(页面黑=视频黑);与 VO/叠加窗无关,mpv 日志 VO 正常具迷惑性
- 修正:单窗透明方案——播放窗 transparent:true 直接加载 React 控制层,mpv 子窗口在下层透出;删除 overlayWin 双窗模型;控制层底色 rgba(0,0,0,.004) 防鼠标穿透
- 验证工具:scripts/screen-probe.ps1(置前+截图+亮度,黑=0/正常=127);mpv.log 常驻 userData
- D14: 播放窗拖动/边缘缩放在 transparent+frame 下表现待用户确认,异常则 frameless+自绘标题栏

## M4 DLNA（已完成,虚拟 DMC 19/19 PASS 20:10）
- `app/dlna/` 零依赖自研协议栈(dlna/ssdp/httpd/xml),utilityProcess 独立进程,主进程 postMessage 桥
- SSDP(M-SEARCH/alive/byebye/网卡变更重宣告)+SOAP 三服务全 action+GENA(LastChange 200ms 去抖,失败×3清理)
- 防 SSRF(仅局域网 http/https)+防 XXE;CASTING 徽标/首页 DLNA 状态已接 UI
- 配置 userData/dlna.json,日志 userData/logs/dlna.log
- 验收:scripts/dmc-test.js(需先 npm start)——19/19 PASS
- 偏差 D15-D20:防火墙自检/托盘待机/SetNextURI/真机矩阵/session表/锁定策略 → M5/P1/用户终验
- 用户已真机验证 M3 全部通过(含 D14)
- 下一步 M5 HDR/打磨:画质决策链、设置页(D10)、视频/高级控制台分页(D11)、外部字幕(D12)、防火墙自检(D15)、托盘(D16)

## M5 HDR 画质决策链（已完成并验证 21:15）
- `app/main/hdr.js` 纯函数决策链,单测 17/17;E2E(hdr-e2e.js) PASS:lavfi 生成 PQ HEVC 测试片,tonemap 全链应用,截图确认 HDR 徽标+画面正常
- 坑1:mpv 0.41 无 display-hdr-peak 属性 → 解析 VO 日志 "Queried output: colorspace:" G2084 判 HDR 显示器
- 坑2:mpv 启动事件(file-loaded/video-reconfig)在管道连接前发出会丢 → connect 后主动拉取+短轮询兜底(M3 轨道菜单同病已修)
- 本机无 HDR 显示器,直通路径(D21/D22)留用户真机终验(决策 4A)
- M5 剩余打磨项(设置页 D10/外部字幕 D12/防火墙自检 D15/托盘 D16)未做,用户只点了 HDR 决策链

## 四问题修复 + 视觉定位（21:40）
1. HDR 直通不可选：检测读 tail 16KB 可能漏早期 Queried output → 漏匹配时全量重读日志;SDR 片源禁用按钮加原因提示（注:HDR 开关后需重启播放,VO 初始化时才查询显示器）
2. 播放窗：顶部 14px app-region:drag 隐形拖拽条;视频就绪前 overlay 黑场遮挡（ready 类切换 rgba(0,0,0,.004)）
3. 浅色全覆盖：--ov-* 播放覆盖层双套令牌 + --cv-* 封面令牌(亮调渐变);规范 §2.5 "恒暗"作废
4. 用户定位:UI 以电影镜头/粒子视觉/舞台为核心的沉浸式播放器 → 已存 feedback_immersive_vision.md;首页加粒子尘光层(46s/64s 双层异速);规范 v1.3
- tsc/build 通过;待用户真机验证

## 播放窗拖动修复（22:40 验证通过）
- app-region:drag 对透明窗无效（Electron 已知限制）→ 手动拖拽：渲染层 mousedown → win:drag-start IPC → 主进程 16ms 轮询 screen.getCursorScreenPoint + setPosition;mouseup → drag-end
- 自动化验证 scripts/drag-test.ps1（SetCursorPos+mouse_event 模拟拖拽,比对窗口位置）：(251,136)→(458,252) PASS
- 坑：Get-Process electron 会选中最小化辅助窗口(-32000,-32000),探针需按标题过滤+SW_RESTORE;ps1 含中文需 UTF8 显式读取执行
- 注意：拖拽时给 top-info 补了 onClick stopPropagation(此前点标题栏会误触暂停)

## 窗口基础能力 + git 仓库（23:50）
- git init 完成,首次提交 M1-M5;第二次提交窗口能力;.gitignore 排除 node_modules/runtime/dist/.agents/.claude
- 8 向边缘缩放热区(10px,DWM 隐形边框吃外侧 ~7px)+ 最小化/最大化按钮;最大化/全屏禁用拖拽缩放
- resize-test.ps1 PASS(east +150/south +100 精确);合成事件测试在应用被关掉后会假失败,先确认实例活着
- 自动化脚本池:pipe-test/pos-test/event-watch/prop-probe/dmc-test/hdr-test/hdr-e2e/screen-probe.ps1/drag-test.ps1/resize-test.ps1

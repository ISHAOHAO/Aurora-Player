# Aqua 深海水主题 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Aurora Player 的整体 UI 仿照 DSH-Transparent-UI-Plugin（Aqua 毛玻璃主题），以 Aqua 为默认外观，保留现有 6 主题系统为可选。

**Architecture:** Aqua 作为第 7 个内置主题加入现有视觉系统（`visual.json` / `registry.ts` / `resolver.ts` / `controller.ts`）。核心 schema 保持不变，Aqua 专属参数放可选的 `theme.aqua` 段；新增 4 个运行时单元（FluidEngine WebGL 流体 + 壁纸、CrittersEngine 海洋生物、SpotlightEngine 聚光+按压、EdgeFadeLayer 边缘雾化），由 controller 统一编排。播放页受透明窗约束：流体 mask 到 letterbox 边缘带，中心露出 mpv。

**Tech Stack:** TypeScript strict · React 19 · Vite 8 · 原生 WebGL（零依赖）· Electron 43。

**验证命令（每任务通用）：**
- `npx tsc --noEmit`（在 `app/` 下）
- `npm run build`（在 `app/` 下）
- `node scripts/visual-test.js`（在 `app/` 下）

## Global Constraints

- 不动 mpv/IPC/DLNA/HDR 决策链/媒体库/播放逻辑/窗口生命周期/托盘/防火墙/硬解（spec §约束）。
- 播放页 Video First 硬约束：`STATE_BUDGETS` 不动；播放页 scene 透明度 = `budgetOpacity * 0.2`；黑场中心 lum ≤0.067、白场中心 ≥0.94。
- 播放页透明窗：DOM `backdrop-filter`/`mix-blend` 无法读取 mpv 视频；流体只出现在 letterbox 边缘带，中心露出视频；视频充满窗口时流体隐藏。
- 业务组件只碰 `VisualProvider` / `useVisual`；不新增 IPC（壁纸用 `<input type="file">` + FileReader，零 IPC）。
- 持久化纪律：slider 直写内存 + debounce 500ms 落盘，Save/SaveAs 立即写。
- 不新增依赖（流体着色器零依赖自研）；reduced-motion 下静默所有氛围运动。
- 代码不写注释（现有文件内已有注释除外，追加代码保持无注释）。
- 官方主题永不落盘；编辑官方 Aqua 自动 clone 为 Custom Copy。

---

### Task 1: Schema 扩展（types.ts + 测试计数基线）

**Files:**
- Modify: `app/renderer/src/visual/types.ts`
- Test: `app/scripts/visual-test-entry.ts`

**Interfaces:**
- Consumes: 现有 `VisualTheme` / `EngineParams` / `BackgroundMode`。
- Produces:
  - `BackgroundMode = 'solid' | 'gradient' | 'cover' | 'fluid' | 'wallpaper' | 'none'`
  - `interface VisualThemeAqua { backdrop: 'fluid'|'wallpaper'; fluidHue: number; fluidDepth: number; bgBrightness: number; wallpaper: string; wallpaperBlur: number; wallpaperFrost: number; edgeFade: boolean; spotlight: boolean; press: boolean; critters: boolean; whale: boolean; }`
  - `VisualTheme.aqua?: VisualThemeAqua`
  - `interface AquaEngineParams extends VisualThemeAqua { enabled: boolean; }`
  - `EngineParams.aqua?: AquaEngineParams`

- [ ] **Step 1: 修改 `types.ts` 扩展 BackgroundMode**

```ts
export type BackgroundMode = 'solid' | 'gradient' | 'cover' | 'fluid' | 'wallpaper' | 'none';
```

- [ ] **Step 2: 在 `types.ts` 中 VisualTheme 接口末尾追加 `aqua?` 字段，并新增两个接口**

在 `VisualTheme` 的 `advanced?` 之后追加：

```ts
  aqua?: VisualThemeAqua;
```

并在文件末尾（`VisualFile` 之后）新增：

```ts
/** Aqua 深海水主题专属参数（可选段；仅 Aqua 及其他需要它的主题填充） */
export interface VisualThemeAqua {
  backdrop: 'fluid' | 'wallpaper';
  fluidHue: number;
  fluidDepth: number;
  bgBrightness: number;
  wallpaper: string;
  wallpaperBlur: number;
  wallpaperFrost: number;
  edgeFade: boolean;
  spotlight: boolean;
  press: boolean;
  critters: boolean;
  whale: boolean;
}

/** EngineParams 的 Aqua 运行时参数（enabled 由 resolver 写入：主题有 aqua 段时 true） */
export interface AquaEngineParams extends VisualThemeAqua {
  enabled: boolean;
}
```

- [ ] **Step 3: 在 `EngineParams` 接口末尾追加可选字段**

```ts
  aqua?: AquaEngineParams;
```

- [ ] **Step 4: 验证类型通过**

Run: `npx tsc --noEmit`（在 `app/`）
Expected: 零错误（现有 6 主题无 aqua 段，无引用处，编译应通过）。

- [ ] **Step 5: 提交**

```bash
git add app/renderer/src/visual/types.ts
git commit -m "feat: 视觉 schema 扩展 Aqua 参数段（fluid/wallpaper/spotlight/critters/edgeFade）"
```

---

### Task 2: Aqua 主题注册（registry.ts + 测试计数）

**Files:**
- Modify: `app/renderer/src/visual/registry.ts`
- Test: `app/scripts/visual-test-entry.ts`

**Interfaces:**
- Consumes: `VisualTheme` + `VisualThemeAqua`（Task 1）。
- Produces: `THEME_IDS` 增加 `'aqua'`；`REGISTRY` 含第 7 个主题 `{ id:'aqua', name:'Aqua', ... }`。

- [ ] **Step 1: 先改测试 —— 更新视觉测试基线计数**

修改 `app/scripts/visual-test-entry.ts`：

```ts
check('registry 7 个官方主题', REGISTRY.length === 7);
check('主题 id 唯一', new Set(REGISTRY.map((t) => t.id)).size === 7);
```

运行确认红（此时 REGISTRY 仍 6 个）：

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: FAIL「registry 7 个官方主题」「主题 id 唯一」；其余 PASS。

- [ ] **Step 2: `registry.ts` 扩展 THEME_IDS**

```ts
export const THEME_IDS = ['cinema', 'aurora', 'noir', 'oled', 'glass', 'immersive', 'aqua'] as const;
```

- [ ] **Step 3: 在 `REGISTRY` 数组末尾（immersive 之后）追加 Aqua 主题**

```ts
  /* ============ Aqua — 深海水主题（DSH-Aqua 风格：毛玻璃浮层 · 流体背景 · 海洋生物 · 光标聚光） ============ */
  t('aqua', 'Aqua', '深海水主题：毛玻璃浮层 · 流体背景 · 海洋生物 · 光标聚光', {
    scene: { mode: 'fluid', bgOpacity: 0.9, cover: { follow: true, locked: false, auto: true } },
    lighting: { enabled: true, intensity: 0.45, blur: 34, spread: 0.55, saturation: 0.75, temperature: 0.8 },
    particles: { enabled: true, density: 0.16, speed: 0.2, size: 0.42, opacity: 0.4, depth: 0.35, reaction: 0.25, color: '#A9C6EF', colorSecondary: '#7EA4DF', glow: 0.18, blur: 1.4, foregroundRatio: 0.02, distribution: 'uneven', motion: 'drift' },
    motion: { enabled: true, camera: 0.2, parallax: 0.3, kenBurns: 0.5, transitionSpeed: 460 },
    atmosphere: { grain: 0.25, bloom: 0.22, vignette: 0.22, aberration: 0.1 },
    ui: { opacity: 0.92, glass: 0.72, blur: 20, border: 0.2, radius: 14, density: 'comfortable', accent: '#6E9BE8' },
    player: { controlOpacity: 0.85, autoHide: 3, metadata: 'normal', defaultPresentation: 'normal' },
    signature: { layout: 'glass', typography: 'clean', surface: 'glass', shape: 'translucent', density: 'medium', motion: 'medium', atmosphere: 'layered' },
    appearance: {
      light: {
        scene: { start: '#F4F8FD', middle: '#EAF1F9', end: '#DCE7F4', angle: 170 },
        base: '#F4F8FD', surface: '#FFFFFF', surface2: '#ECF2FA', text: '#13243E',
        accent: '#3F76D8', bgOpacity: 0.4, lightingIntensity: 0.5, grain: 0.2, vignette: 0.1,
      },
    },
    aqua: {
      backdrop: 'fluid', fluidHue: 320, fluidDepth: 25, bgBrightness: 50,
      wallpaper: '', wallpaperBlur: 0, wallpaperFrost: 0,
      edgeFade: true, spotlight: true, press: true, critters: true, whale: true,
    },
  }),
```

- [ ] **Step 4: 更新视觉测试中依赖主题数的断言**

`app/scripts/visual-test-entry.ts` 中：

```ts
check('7 主题 dark accent 各自不同', accents.size === 7);
```

```ts
check('14 组合 text/bg 对比可读（≥60 亮度差）', okC === 14);
```

并将 state-sync 循环补上 aqua：

```ts
  for (const id of ['cinema', 'aurora', 'noir', 'oled', 'glass', 'immersive', 'aqua']) {
```

- [ ] **Step 5: 运行测试验证全绿**

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: 全部 PASS，`RESULT ok=... fail=0`。

- [ ] **Step 6: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 7: 提交**

```bash
git add app/renderer/src/visual/registry.ts app/scripts/visual-test-entry.ts
git commit -m "feat: 注册 Aqua 第 7 内置主题（深海水毛玻璃）"
```

---

### Task 3: Resolver Aqua 令牌（resolver.ts）

**Files:**
- Modify: `app/renderer/src/visual/resolver.ts`
- Test: `app/scripts/visual-test-entry.ts`

**Interfaces:**
- Consumes: `VisualTheme`（含 aqua 段）、`EngineParams`（含 aqua 可选）。
- Produces: `resolve()` 输出：
  - cssVars 新增 `--vs-frost` / `--vs-glass-card-light` / `--vs-glass-card-dark` / `--vs-aqua-hue` / `--vs-aqua-depth` / `--vs-aqua-brightness-black` / `--vs-aqua-brightness-white` / `--vs-aqua-wallpaper-blur` / `--vs-aqua-wallpaper-frost` / `--vs-aqua-spot-color`。
  - `engineParams.aqua: AquaEngineParams`（仅主题有 aqua 段时设置）。
  - `sceneOf()` 对 `mode==='fluid'` 返回深海海军蓝基础渐变（流体 canvas 的兜底底色）。

- [ ] **Step 1: 先写测试 —— resolver 对 Aqua 产出 aqua 令牌**

在 `app/scripts/visual-test-entry.ts` 的 resolver 区块后追加：

```ts
/* 2g. Aqua 主题：aqua 段 → 令牌 + engineParams.aqua */
{
  const a = getBuiltinTheme('aqua')!;
  check('Aqua 主题含 aqua 段', !!a.aqua);
  const dark = resolve(a, { state: 'browse', appearance: 'dark' });
  check('Aqua dark 有 --vs-glass-card-dark', (dark.cssVars['--vs-glass-card-dark'] || '').includes('linear-gradient'));
  check('Aqua dark 有 --vs-frost', Number(dark.cssVars['--vs-frost']) > 0);
  check('Aqua engineParams.aqua.enabled', dark.engineParams.aqua?.enabled === true);
  check('Aqua aqua.fluidHue 传递', dark.engineParams.aqua?.fluidHue === 320);
  check('Aqua scene mode=fluid 有兜底底色', (dark.cssVars['--vs-bg'] || '').length > 0);
  const light = resolve(a, { state: 'browse', appearance: 'light' });
  check('Aqua light 有 --vs-glass-card-light', (light.cssVars['--vs-glass-card-light'] || '').includes('linear-gradient'));
  const c = getBuiltinTheme('cinema')!;
  check('Cinema 无 aqua 引擎参数', resolve(c, { state: 'browse', appearance: 'dark' }).engineParams.aqua == null);
}
```

运行确认红：

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: FAIL 以上 aqua 断言（resolver 尚未实现）。

- [ ] **Step 2: `sceneOf()` 支持 fluid / wallpaper 模式**

修改 `sceneOf()` 中 `// dark` 分支，在 `s.mode === 'cover'` 之前插入：

```ts
  if (s.mode === 'fluid' || s.mode === 'wallpaper') {
    return { css: 'linear-gradient(160deg, #0C121B, #0A0F18 55%, #080C14)', base: '#0C121B' };
  }
```

- [ ] **Step 3: resolve() 末尾（engineParams 组装后）追加 Aqua 令牌写入**

在 `return { cssVars, engineParams };` 之前插入：

```ts
  // —— Aqua 专属令牌（theme.aqua 存在时）——
  const A = theme.aqua;
  if (A) {
    const frost = Math.min(1.4, Math.max(0.05, ui.glass * 1.4));
    cssVars['--vs-frost'] = String(frost);
    cssVars['--vs-glass-card-light'] =
      'linear-gradient(180deg, color-mix(in srgb, rgb(255 255 255) calc(50% * var(--vs-frost,1)), transparent), color-mix(in srgb, rgb(255 255 255) calc(35% * var(--vs-frost,1)), transparent))';
    cssVars['--vs-glass-card-dark'] =
      'linear-gradient(180deg, color-mix(in srgb, rgb(42 46 56) calc(50% * var(--vs-frost,1)), transparent), color-mix(in srgb, rgb(22 25 34) calc(50% * var(--vs-frost,1)), transparent))';
    cssVars['--vs-aqua-hue'] = String(A.fluidHue);
    cssVars['--vs-aqua-depth'] = String(A.fluidDepth / 100);
    const darkBright = appearance === 'dark';
    cssVars['--vs-aqua-brightness-black'] = String(darkBright ? Math.max(0, (50 - A.bgBrightness) / 50) : 0);
    cssVars['--vs-aqua-brightness-white'] = String(darkBright ? 0 : Math.max(0, (A.bgBrightness - 50) / 50));
    cssVars['--vs-aqua-wallpaper-blur'] = `${A.wallpaperBlur}px`;
    cssVars['--vs-aqua-wallpaper-frost'] = String(A.wallpaperFrost / 100);
    const glowHue = ((A.fluidHue + 320) % 360 + 360) % 360;
    cssVars['--vs-aqua-spot-color'] = darkBright
      ? `hsla(${glowHue}, 90%, 62%, 0.17)`
      : `hsla(${glowHue}, 90%, 45%, 0.16)`;

    engineParams.aqua = {
      enabled: true,
      backdrop: A.backdrop,
      fluidHue: A.fluidHue,
      fluidDepth: A.fluidDepth,
      bgBrightness: A.bgBrightness,
      wallpaper: A.wallpaper,
      wallpaperBlur: A.wallpaperBlur,
      wallpaperFrost: A.wallpaperFrost,
      edgeFade: A.edgeFade,
      spotlight: A.spotlight,
      press: A.press,
      critters: A.critters,
      whale: A.whale,
    };
  }
```

- [ ] **Step 4: 运行测试验证全绿**

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: 全部 PASS，`RESULT ok=... fail=0`。

- [ ] **Step 5: 类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误。

- [ ] **Step 6: 提交**

```bash
git add app/renderer/src/visual/resolver.ts app/scripts/visual-test-entry.ts
git commit -m "feat: resolver 产出 Aqua 玻璃卡片/流体/聚光/亮度令牌 + engineParams.aqua"
```

---

### Task 4: store 默认 Aqua + 迁移兜底

**Files:**
- Modify: `app/renderer/src/visual/store.ts`
- Test: `app/scripts/visual-test-entry.ts`

**Interfaces:**
- Consumes: `VisualStore` 现有结构。
- Produces: 默认 `activeThemeId = 'aqua'`；`removePreset` / `resetActive` 兜底 `'aqua'`；`migrateFromVisualMode` 无 visualMode / 无映射时落 `'aqua'`；已有 `visual.json` 用户不受影响。

- [ ] **Step 1: 先改测试 —— store 默认 Aqua 断言**

`app/scripts/visual-test-entry.ts` 中：

```ts
  await VisualStore.resetActive();
  check('reset 回 Aqua', VisualStore.getActiveMeta().name === 'Aqua');
```

```ts
  await VisualStore.resetActive();
  check('reset 后 identity 回 Aqua', VisualStore.activeThemeId === 'aqua');
```

运行确认红：

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: FAIL 这两个 reset 断言（store 仍回 Cinema）。

- [ ] **Step 2: `store.ts` 初始默认改 aqua**

```ts
let activeThemeId = 'aqua';
let presets: VisualTheme[] = [];
let snapshot: Snapshot = { activeThemeId: 'aqua', activeName: 'Aqua', activeSource: 'builtin', revision: 0 };
```

- [ ] **Step 3: 兜底值统一改 aqua**

```ts
async function removePreset(id: string): Promise<void> {
  presets = presets.filter((x) => x.id !== id);
  if (activeThemeId === id) { activeThemeId = 'aqua'; }
  await flush();
  emit();
}
```

```ts
async function resetActive(): Promise<void> {
  activeThemeId = 'aqua';
  await flush();
  emit();
}
```

- [ ] **Step 4: init 兜底 + 迁移兜底改 aqua**

```ts
    if (!getBuiltinTheme(activeThemeId) && !isUserPreset(activeThemeId)) activeThemeId = 'aqua';
```

```ts
    const mode = st.visualMode || 'aqua';
```

```ts
      activeThemeId = map[mode] ?? 'aqua';
```

```ts
  } catch {
    activeThemeId = 'aqua';
  }
```

（保留 `map` 中旧 visualMode → 主题 id 的映射不变，仅兜底改 aqua。）

- [ ] **Step 5: 运行测试验证全绿**

Run: `node scripts/visual-test.js`（在 `app/`）
Expected: 全部 PASS，`RESULT ok=... fail=0`。

- [ ] **Step 6: 类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误。

- [ ] **Step 7: 提交**

```bash
git add app/renderer/src/visual/store.ts app/scripts/visual-test-entry.ts
git commit -m "feat: Aqua 为默认主题，迁移/重置兜底改 aqua（已有用户保持当前主题）"
```

---

### Task 5: FluidEngine（WebGL 流体 + 壁纸）

**Files:**
- Create: `app/renderer/src/visual/engines/FluidEngine.ts`
- Modify: `app/renderer/src/visual/controller.ts`（挂载 + apply 接线，见 Task 9 —— 本任务先独立实现引擎类并自测）

**Interfaces:**
- Consumes: `AquaEngineParams`（Task 1）。
- Produces:
  - `class FluidEngine`
    - `mount(container: HTMLElement): void` —— 创建 `div.vs-fluid`（内含 `canvas.vs-fluid-canvas` + `div.vs-fluid-wallpaper`），WebGL 失败时静默降级（仅 canvas 不启动，CSS 兜底色仍显示）。
    - `apply(p: AquaEngineParams | undefined): void` —— 切换 fluid/wallpaper 源、写 hue/depth/brightness/wallpaper 变量、启动/停止 rAF。
    - `setVideoRect(rect: { top: number; bottom: number; left: number; right: number } | null): void` —— 播放页 letterbox mask（null=全窗）。
    - `setPointer(x: number, y: number): void`
    - `unmount(): void`

- [ ] **Step 1: 创建 `FluidEngine.ts`**

```ts
import type { AquaEngineParams } from '../types';

const REDUCE = typeof matchMedia !== 'undefined' && matchMedia('(prefers-reduced-motion: reduce)').matches;

const VERT = `attribute vec2 aPos;void main(){gl_Position=vec4(aPos,0.,1.);}`;

const FRAG = `precision highp float;
uniform vec2 uRes;uniform float uTime;uniform float uHue;uniform float uDepth;
float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453123);}
float noise(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.-2.*f);return mix(mix(hash(i),hash(i+vec2(1.,0.)),f.x),mix(hash(i+vec2(0.,1.)),hash(i+vec2(1.,1.)),f.x),f.y);}
float fbm(vec2 p){float v=0.,a=.5;for(int i=0;i<5;i++){v+=a*noise(p);p*=2.03;a*=.5;}return v;}
vec3 hsv2rgb(vec3 c){vec3 p=abs(fract(c.xxx+vec3(0.,2./3.,1./3.))*6.-3.);return c.z*mix(vec3(1.),clamp(p-1.,0.,1.),c.y);}
void main(){
  vec2 uv=gl_FragCoord.xy/uRes.xy;
  vec2 p=uv; p.x*=uRes.x/uRes.y;
  float t=uTime*.02;
  float q1=fbm(p*1.3+vec2(t*.5,-t*.35));
  float q2=fbm(p*2.1-vec2(t*.45,t*.3)+q1*.9);
  float q3=fbm(p*3.2+vec2(t*.3,t*.5)+q2*.6);
  float mixW=clamp((q1+q2+q3)/3.,0.,1.);
  float sat=mix(.55,.1,uDepth);
  float val=mix(.28,.6,uDepth);
  float h1=(uHue+q1*42.)/360.;
  float h2=(uHue+48.+q2*48.)/360.;
  float h3=(uHue-28.+q3*38.)/360.;
  vec3 c1=hsv2rgb(vec3(fract(h1),sat,val));
  vec3 c2=hsv2rgb(vec3(fract(h2),sat*.8,val*1.08));
  vec3 c3=hsv2rgb(vec3(fract(h3),sat*.6,val*.82));
  vec3 col=mix(c3,mix(c1,c2,clamp(q2*1.5,0.,1.)),mixW);
  col+=vec3(.45,.55,.72)*(1.-uv.y)*.05;
  col=pow(col,vec3(.86));
  gl_FragColor=vec4(col,1.);
}`;

export class FluidEngine {
  private root: HTMLDivElement | null = null;
  private canvas: HTMLCanvasElement | null = null;
  private wp: HTMLDivElement | null = null;
  private gl: WebGLRenderingContext | null = null;
  private prog: WebGLProgram | null = null;
  private uniTime = -1; private uniHue = -1; private uniDepth = -1; private uniRes = -1;
  private raf = 0;
  private last = 0;
  private running = false;
  private cfg: AquaEngineParams | null = null;
  private rect: { top: number; bottom: number; left: number; right: number } | null = null;
  private hue = 320;
  private depth = 0.25;
  private pointer = { x: -9999, y: -9999 };

  mount(container: HTMLElement): void {
    if (this.root) return;
    this.root = document.createElement('div');
    this.root.className = 'vs-fluid';
    this.canvas = document.createElement('canvas');
    this.canvas.className = 'vs-fluid-canvas';
    this.wp = document.createElement('div');
    this.wp.className = 'vs-fluid-wallpaper';
    this.root.appendChild(this.canvas);
    this.root.appendChild(this.wp);
    container.appendChild(this.root);
    this.initGL();
    window.addEventListener('resize', this.onResize);
    document.addEventListener('visibilitychange', this.onVis);
  }
  unmount(): void {
    this.stop();
    window.removeEventListener('resize', this.onResize);
    document.removeEventListener('visibilitychange', this.onVis);
    this.root?.remove(); this.root = null; this.canvas = null; this.wp = null; this.gl = null; this.prog = null;
  }

  private onResize = () => { this.resize(); };
  private onVis = () => { if (document.hidden) this.stop(); else if (this.running && this.cfg) this.start(); };

  private initGL(): void {
    const c = this.canvas;
    if (!c) return;
    const gl = c.getContext('webgl', { antialias: false, depth: false, stencil: false, alpha: true, premultipliedAlpha: true });
    if (!gl) return;
    this.gl = gl;
    const compile = (type: number, src: string): WebGLShader | null => {
      const sh = gl.createShader(type);
      if (!sh) return null;
      gl.shaderSource(sh, src);
      gl.compileShader(sh);
      if (!gl.getShaderParameter(sh, gl.COMPILE_STATUS)) { gl.deleteShader(sh); return null; }
      return sh;
    };
    const vs = compile(gl.VERTEX_SHADER, VERT);
    const fs = compile(gl.FRAGMENT_SHADER, FRAG);
    if (!vs || !fs) return;
    const prog = gl.createProgram();
    if (!prog) return;
    gl.attachShader(prog, vs);
    gl.attachShader(prog, fs);
    gl.linkProgram(prog);
    if (!gl.getProgramParameter(prog, gl.LINK_STATUS)) return;
    this.prog = prog;
    gl.useProgram(prog);
    const buf = gl.createBuffer();
    gl.bindBuffer(gl.ARRAY_BUFFER, buf);
    gl.bufferData(gl.ARRAY_BUFFER, new Float32Array([-1, -1, 3, -1, -1, 3]), gl.STATIC_DRAW);
    const loc = gl.getAttribLocation(prog, 'aPos');
    gl.enableVertexAttribArray(loc);
    gl.vertexAttribPointer(loc, 2, gl.FLOAT, false, 0, 0);
    this.uniTime = gl.getUniformLocation(prog, 'uTime') ?? -1;
    this.uniHue = gl.getUniformLocation(prog, 'uHue') ?? -1;
    this.uniDepth = gl.getUniformLocation(prog, 'uDepth') ?? -1;
    this.uniRes = gl.getUniformLocation(prog, 'uRes') ?? -1;
    this.resize();
  }

  private resize(): void {
    const c = this.canvas, gl = this.gl;
    if (!c || !gl) return;
    const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
    c.width = Math.floor(window.innerWidth * dpr);
    c.height = Math.floor(window.innerHeight * dpr);
    gl.viewport(0, 0, c.width, c.height);
  }

  apply(p: AquaEngineParams | undefined): void {
    if (!p || !p.enabled) { this.stop(); this.hide(); return; }
    this.cfg = p;
    this.hue = p.fluidHue;
    this.depth = p.fluidDepth / 100;
    this.show(p.backdrop === 'fluid');
    if (p.backdrop === 'wallpaper') this.applyWallpaper(p);
    else this.applyBrightness(p.bgBrightness);
    if (!REDUCE) this.start();
  }

  private hide(): void { if (this.root) this.root.style.display = 'none'; }
  private show(fluid: boolean): void {
    if (!this.root || !this.canvas || !this.wp) return;
    this.root.style.display = 'block';
    this.canvas.style.display = fluid ? 'block' : 'none';
    this.wp.style.display = fluid ? 'none' : 'block';
  }

  private applyWallpaper(p: AquaEngineParams): void {
    const w = this.wp;
    if (!w) return;
    let img = w.querySelector('img') as HTMLImageElement | null;
    if (!img) { img = document.createElement('img'); w.appendChild(img); }
    img.className = 'vs-fluid-wallpaper-img';
    if (p.wallpaper) img.src = p.wallpaper; else img.removeAttribute('src');
    w.style.setProperty('--vs-aqua-wallpaper-blur', `${p.wallpaperBlur}px`);
    w.style.setProperty('--vs-aqua-wallpaper-frost', String(p.wallpaperFrost / 100));
    this.applyBrightness(p.bgBrightness);
  }

  private applyBrightness(bg: number): void {
    if (!this.root) return;
    const dark = document.documentElement.dataset.theme !== 'light';
    this.root.style.setProperty('--vs-aqua-brightness-black', String(dark ? Math.max(0, (50 - bg) / 50) : 0));
    this.root.style.setProperty('--vs-aqua-brightness-white', String(dark ? 0 : Math.max(0, (bg - 50) / 50)));
  }

  setVideoRect(rect: { top: number; bottom: number; left: number; right: number } | null): void {
    this.rect = rect;
    if (!this.root) return;
    if (!rect) { this.root.style.clipPath = ''; return; }
    const t = Math.round(rect.top), b = Math.round(rect.bottom), l = Math.round(rect.left), r = Math.round(rect.right);
    if (t === 0 && b === window.innerHeight && l === 0 && r === window.innerWidth) {
      this.root.style.clipPath = 'polygon(0 0, 0 0, 0 0, 0 0, 100% 100%, 100% 100%, 100% 100%, 100% 100%)';
      return;
    }
    if (t > 0 || b < window.innerHeight) {
      this.root.style.clipPath = `polygon(0 0, 100% 0, 100% ${t}px, 0 ${t}px, 0 ${b}px, 100% ${b}px, 100% 100%, 0 100%)`;
    } else {
      this.root.style.clipPath = `polygon(0 0, ${l}px 0, ${l}px 100%, 0 100%, ${r}px 0, 100% 0, 100% 100%, ${r}px 100%)`;
    }
  }

  setPointer(x: number, y: number): void { this.pointer.x = x; this.pointer.y = y; }

  private start(): void {
    if (this.running || !this.gl || !this.prog) return;
    this.running = true;
    this.last = 0;
    this.raf = requestAnimationFrame(this.tick);
  }
  private stop(): void {
    this.running = false;
    cancelAnimationFrame(this.raf);
  }

  private tick = (t: number): void => {
    this.raf = requestAnimationFrame(this.tick);
    const gl = this.gl, prog = this.prog;
    if (!this.running || !gl || !prog) return;
    const dt = Math.min(50, t - this.last || 16); this.last = t;
    gl.uniform1f(this.uniTime, t / 1000);
    gl.uniform1f(this.uniHue, this.hue);
    gl.uniform1f(this.uniDepth, this.depth);
    gl.uniform2f(this.uniRes, this.canvas?.width || 1, this.canvas?.height || 1);
    gl.drawArrays(gl.TRIANGLES, 0, 3);
    void dt;
  };

  get element(): HTMLDivElement | null { return this.root; }
}
```

- [ ] **Step 2: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/visual/engines/FluidEngine.ts
git commit -m "feat: FluidEngine WebGL 流体板 + 壁纸层（零依赖，letterbox clip-path mask）"
```

---

### Task 6: CrittersEngine（鲸鱼 + 海洋生物）

**Files:**
- Create: `app/renderer/src/visual/engines/CrittersEngine.ts`
- Modify: `app/renderer/src/visual/visual.css`（critter 动画，见 Task 10 —— 本任务先实现引擎类）

**Interfaces:**
- Consumes: `AquaEngineParams`。
- Produces:
  - `class CrittersEngine`
    - `mount(container: HTMLElement): void` —— 创建 `div.vs-critters`（含鲸鱼 canvas + 一组 critter 元素）。
    - `apply(p: AquaEngineParams | undefined, onPlayer: boolean): void` —— 启用/禁用；播放页隐藏鲸鱼（中心），critters 保留但降透明度。
    - `unmount(): void`

- [ ] **Step 1: 创建 `CrittersEngine.ts`**

```ts
import type { AquaEngineParams } from '../types';

const REDUCE = typeof matchMedia !== 'undefined' && matchMedia('(prefers-reduced-motion: reduce)').matches;

interface Critter { el: HTMLSpanElement; kind: string; }

export class CrittersEngine {
  private root: HTMLDivElement | null = null;
  private whaleCanvas: HTMLCanvasElement | null = null;
  private whaleCtx: CanvasRenderingContext2D | null = null;
  private critters: Critter[] = [];
  private raf = 0;
  private running = false;
  private last = 0;
  private on = false;
  private onPlayer = false;

  mount(container: HTMLElement): void {
    if (this.root) return;
    this.root = document.createElement('div');
    this.root.className = 'vs-critters';
    this.whaleCanvas = document.createElement('canvas');
    this.whaleCanvas.className = 'vs-whale';
    this.root.appendChild(this.whaleCanvas);
    this.whaleCtx = this.whaleCanvas.getContext('2d');
    const kinds: [string, number][] = [
      ['fish', 2], ['fish-left', 1], ['bubble', 4], ['plankton', 5],
    ];
    for (const [kind, n] of kinds) {
      for (let i = 0; i < n; i++) {
        const el = document.createElement('span');
        el.className = 'vs-critter';
        el.dataset.kind = kind;
        el.textContent = kind.startsWith('bubble') ? '○' : kind === 'fish' || kind === 'fish-left' ? '◀' : '·';
        el.style.left = `${6 + Math.random() * 88}%`;
        el.style.top = `${10 + Math.random() * 78}%`;
        el.style.animationDelay = `${-Math.random() * 20}s`;
        el.style.animationDuration = `${8 + Math.random() * 12}s`;
        this.root.appendChild(el);
        this.critters.push({ el, kind });
      }
    }
    container.appendChild(this.root);
    window.addEventListener('resize', this.onResize);
  }
  unmount(): void {
    this.stop();
    window.removeEventListener('resize', this.onResize);
    this.root?.remove(); this.root = null; this.whaleCanvas = null; this.whaleCtx = null; this.critters = [];
  }

  apply(p: AquaEngineParams | undefined, onPlayer: boolean): void {
    this.onPlayer = onPlayer;
    const show = !!(p && p.enabled && p.critters);
    const showWhale = show && p?.whale && !onPlayer;
    if (this.root) this.root.style.display = show ? 'block' : 'none';
    if (this.whaleCanvas) this.whaleCanvas.style.display = showWhale ? 'block' : 'none';
    if (this.root && onPlayer) this.root.style.opacity = '0.5';
    if (showWhale) this.start();
    else this.stop();
  }

  private onResize = () => { this.resizeWhale(); };

  private resizeWhale(): void {
    const c = this.whaleCanvas, ctx = this.whaleCtx;
    if (!c || !ctx) return;
    const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
    const size = Math.min(window.innerWidth, window.innerHeight) * 0.36;
    c.width = Math.floor(size * dpr);
    c.height = Math.floor(size * 0.6 * dpr);
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    ctx.clearRect(0, 0, c.width, c.height);
  }

  private start(): void {
    if (this.running) return;
    this.running = true;
    this.last = 0;
    this.raf = requestAnimationFrame(this.tick);
  }
  private stop(): void {
    this.running = false;
    cancelAnimationFrame(this.raf);
  }

  private tick = (t: number): void => {
    this.raf = requestAnimationFrame(this.tick);
    const c = this.whaleCanvas, ctx = this.whaleCtx;
    if (!this.running || !c || !ctx) return;
    const dt = Math.min(50, t - this.last || 16); this.last = t;
    ctx.clearRect(0, 0, c.width, c.height);
    const dark = document.documentElement.dataset.theme !== 'light';
    const col = dark ? 'rgba(220,232,250,0.55)' : 'rgba(38,58,92,0.45)';
    const w = c.width, h = c.height;
    const cx = w / 2, cy = h / 2;
    ctx.fillStyle = col;
    const n = 110;
    for (let i = 0; i < n; i++) {
      const ph = (i / n) * Math.PI * 2;
      const und = Math.sin(ph * 3 + t * 0.001) * h * 0.045;
      const body = Math.sin(ph) * Math.PI;
      const rad = (body > 0 ? Math.sin(body) : 0) * w * 0.3;
      const tail = body < 0 ? Math.sin(body * 2.2) * w * 0.12 : 0;
      const px = cx + Math.cos(ph) * w * 0.34 + (body < 0 ? tail : 0);
      const py = cy + Math.sin(ph) * h * 0.22 + und;
      ctx.beginPath();
      ctx.arc(px, py, 1.1 + rad * 0.012, 0, Math.PI * 2);
      ctx.fill();
    }
    void dt;
  };

  get element(): HTMLDivElement | null { return this.root; }
}
```

- [ ] **Step 2: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/visual/engines/CrittersEngine.ts
git commit -m "feat: CrittersEngine 鲸鱼 canvas + 鱼/气泡/浮游 CSS 生物"
```

---

### Task 7: SpotlightEngine（光标聚光 + hover 按压）

**Files:**
- Create: `app/renderer/src/visual/engines/SpotlightEngine.ts`

**Interfaces:**
- Consumes: `AquaEngineParams`。
- Produces:
  - `class SpotlightEngine`
    - `mount(): void` —— 全局 `pointermove` / `pointerleave` 监听。
    - `apply(p: AquaEngineParams | undefined): void` —— 启用聚光/按压；给已知玻璃卡选择器 stamp `data-vs-spot` / `data-vs-press`（seam-stamper 思路，不侵入 React）。
    - `unmount(): void`

- [ ] **Step 1: 创建 `SpotlightEngine.ts`**

```ts
import type { AquaEngineParams } from '../types';

const REDUCE = typeof matchMedia !== 'undefined' && matchMedia('(prefers-reduced-motion: reduce)').matches;

const SPOT_SELECTORS = [
  '.topbar', '.hero-card', '.tile', '.poster .cover', '.url-modal', '.nas-browser',
  '.control-deck', '.top-info', '.pop-menu', '.console-drawer', '.cast-toast', '.win-float',
  '.settings-topbar', '.settings-nav',
];

export class SpotlightEngine {
  private on = false;
  private press = false;
  private glowMap = new Map<Element, HTMLDivElement>();
  private x = -9999;
  private y = -9999;

  mount(): void {
    window.addEventListener('pointermove', this.onMove, { passive: true });
    window.addEventListener('pointerdown', this.onMove, { passive: true });
  }
  unmount(): void {
    window.removeEventListener('pointermove', this.onMove);
    window.removeEventListener('pointerdown', this.onMove);
    this.teardown();
  }

  apply(p: AquaEngineParams | undefined): void {
    const spot = !!(p && p.enabled && p.spotlight);
    const press = !!(p && p.enabled && p.press) && !REDUCE;
    this.on = spot;
    this.press = press;
    this.stamp();
    if (!spot) this.teardown();
  }

  private stamp(): void {
    if (!this.on) return;
    const found = new Set<Element>();
    for (const sel of SPOT_SELECTORS) {
      for (const el of Array.from(document.querySelectorAll(sel))) {
        found.add(el);
        if (el.getAttribute('data-vs-spot') === null) {
          el.setAttribute('data-vs-spot', '');
          const glow = document.createElement('div');
          glow.className = 'vs-spot-glow';
          el.appendChild(glow);
          this.glowMap.set(el, glow);
        }
      }
    }
    for (const el of Array.from(found)) {
      if (this.press && el.getAttribute('data-vs-press') === null) el.setAttribute('data-vs-press', '');
    }
  }

  private teardown(): void {
    for (const [el, glow] of this.glowMap) { glow.remove(); el.removeAttribute('data-vs-spot'); el.removeAttribute('data-vs-press'); }
    this.glowMap.clear();
  }

  private onMove = (e: PointerEvent): void => {
    this.x = e.clientX;
    this.y = e.clientY;
    if (!this.on) return;
    for (const [el, glow] of this.glowMap) {
      const r = el.getBoundingClientRect();
      const inside = this.x >= r.left && this.x <= r.right && this.y >= r.top && this.y <= r.bottom;
      if (!inside) {
        glow.style.opacity = '0';
        if (this.press) el.style.transform = '';
        continue;
      }
      const px = this.x - r.left, py = this.y - r.top;
      glow.style.background = `radial-gradient(420px 240px at ${px}px ${py}px, var(--vs-aqua-spot-color, rgba(110,155,232,.16)), transparent 70%)`;
      glow.style.opacity = '1';
      if (this.press && r.width > 0 && r.height > 0) {
        const rx = ((py / r.height) - 0.5) * -3;
        const ry = ((px / r.width) - 0.5) * 3;
        el.style.transform = `perspective(800px) rotateX(${rx.toFixed(2)}deg) rotateY(${ry.toFixed(2)}deg)`;
      }
    }
  };
}
```

- [ ] **Step 2: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/visual/engines/SpotlightEngine.ts
git commit -m "feat: SpotlightEngine 光标聚光 + hover 3D 按压（seam-stamp 不侵入 React）"
```

---

### Task 8: EdgeFadeLayer（边缘雾化）

**Files:**
- Create: `app/renderer/src/visual/engines/EdgeFadeLayer.ts`

**Interfaces:**
- Consumes: `AquaEngineParams`。
- Produces:
  - `class EdgeFadeLayer`
    - `mount(): void` —— 挂 `div#vs-edge-top` / `div#vs-edge-bottom` 到 body。
    - `apply(p: AquaEngineParams | undefined, onPlayer: boolean): void` —— 启用（非播放页）/禁用；播放页隐藏。
    - `unmount(): void`

- [ ] **Step 1: 创建 `EdgeFadeLayer.ts`**

```ts
import type { AquaEngineParams } from '../types';

export class EdgeFadeLayer {
  private top: HTMLDivElement | null = null;
  private bottom: HTMLDivElement | null = null;

  mount(): void {
    if (this.top) return;
    this.top = document.createElement('div');
    this.top.id = 'vs-edge-top';
    this.top.setAttribute('data-vs-edge', 'top');
    this.bottom = document.createElement('div');
    this.bottom.id = 'vs-edge-bottom';
    this.bottom.setAttribute('data-vs-edge', 'bottom');
    document.body.appendChild(this.top);
    document.body.appendChild(this.bottom);
  }
  unmount(): void {
    this.top?.remove(); this.top = null;
    this.bottom?.remove(); this.bottom = null;
  }

  apply(p: AquaEngineParams | undefined, onPlayer: boolean): void {
    const show = !!(p && p.enabled && p.edgeFade) && !onPlayer;
    if (this.top) this.top.style.display = show ? 'block' : 'none';
    if (this.bottom) this.bottom.style.display = show ? 'block' : 'none';
  }

  get topEl(): HTMLDivElement | null { return this.top; }
  get bottomEl(): HTMLDivElement | null { return this.bottom; }
}
```

- [ ] **Step 2: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/visual/engines/EdgeFadeLayer.ts
git commit -m "feat: EdgeFadeLayer 顶/底边缘雾化带"
```

---

### Task 9: controller 编排 + 视频矩形 mask + PlaybackProbe 扩展

**Files:**
- Modify: `app/renderer/src/visual/controller.ts`
- Modify: `app/renderer/src/visual/playback.ts`
- Modify: `app/renderer/src/pages/Player.tsx`（推送视频宽高）

**Interfaces:**
- Consumes: FluidEngine / CrittersEngine / SpotlightEngine / EdgeFadeLayer（Task 5-8）、`AquaEngineParams`、`PlaybackProbe`。
- Produces:
  - `PlaybackInfo` 增加 `videoW: number | null; videoH: number | null`。
  - controller 在 `initVisualSystem` mount 四个新引擎，在 `applyEngines` 接线 apply；计算视频 letterbox 矩形并调 `fluid.setVideoRect`。

- [ ] **Step 1: `playback.ts` 扩展 PlaybackInfo**

```ts
export interface PlaybackInfo {
  playing: boolean;
  paused: boolean;
  idle: boolean;
  fullscreen: boolean;
  title: string | null;
  fileId: string | null;
  time: number;
  coverUrl: string | null;
  videoW: number | null;
  videoH: number | null;
}
```

```ts
let state: PlaybackInfo = {
  playing: false, paused: true, idle: false, fullscreen: false,
  title: null, fileId: null, time: 0, coverUrl: null,
  videoW: null, videoH: null,
};
```

- [ ] **Step 2: `Player.tsx` 推送视频宽高**

在现有 `PlaybackProbe.update({...})` 中追加：

```ts
      videoW: status?.stats?.w ?? null,
      videoH: status?.stats?.h ?? null,
```

并在卸载时清空（返回首页不残留）：

```ts
  useEffect(() => () => { PlaybackProbe.update({ videoW: null, videoH: null }); }, []);
```

- [ ] **Step 3: `controller.ts` 导入并实例化新引擎**

```ts
import { FluidEngine } from './engines/FluidEngine';
import { CrittersEngine } from './engines/CrittersEngine';
import { SpotlightEngine } from './engines/SpotlightEngine';
import { EdgeFadeLayer } from './engines/EdgeFadeLayer';
```

```ts
const fluid = new FluidEngine();
const critters = new CrittersEngine();
const spotlight = new SpotlightEngine();
const edgeFade = new EdgeFadeLayer();
```

- [ ] **Step 4: `initVisualSystem` 挂载新引擎**

在 `particles.mount(root);` 后追加：

```ts
  fluid.mount(root);
  critters.mount(root);
  edgeFade.mount();
  spotlight.mount();
```

- [ ] **Step 5: `applyEngines` 接线**

```ts
function applyEngines(p: EngineParams, state: RouteState): void {
  background.apply(p.scene);
  atmosphere.apply({ grain: p.grain, vignette: p.vignette, bloom: p.bloom }, p.motion.transition);
  lighting.apply(p.light, p.motion.transition);
  const cap = state === 'playback' || state === 'immersive' ? 180 : state === 'browse' ? 60 : 0;
  particles.apply(p.particles, cap);
  motion.apply(p.motion);
  critters.apply(p.aqua, state === 'playback' || state === 'immersive');
  edgeFade.apply(p.aqua, state === 'playback' || state === 'immersive');
  spotlight.apply(p.aqua);
  fluid.apply(p.aqua);
  const probe = PlaybackProbe.get();
  const aspect = probe.videoW && probe.videoH ? probe.videoW / probe.videoH : null;
  const winAspect = window.innerWidth / window.innerHeight;
  let rect: { top: number; bottom: number; left: number; right: number } | null = null;
  if (state === 'playback' || state === 'immersive') {
    if (aspect && Math.abs(aspect - winAspect) > 0.01) {
      if (aspect > winAspect) {
        const h = window.innerWidth / aspect;
        rect = { top: (window.innerHeight - h) / 2, bottom: (window.innerHeight + h) / 2, left: 0, right: window.innerWidth };
      } else {
        const w = window.innerHeight * aspect;
        rect = { top: 0, bottom: window.innerHeight, left: (window.innerWidth - w) / 2, right: (window.innerWidth + w) / 2 };
      }
    }
  }
  fluid.setVideoRect(rect);
}
```

- [ ] **Step 6: `destroyVisualSystem` 反挂载**

在 `motion.destroy();` 后追加：

```ts
  fluid.unmount();
  critters.unmount();
  spotlight.unmount();
  edgeFade.unmount();
```

- [ ] **Step 7: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 8: 提交**

```bash
git add app/renderer/src/visual/controller.ts app/renderer/src/visual/playback.ts app/renderer/src/pages/Player.tsx
git commit -m "feat: controller 编排 Aqua 引擎 + 播放页 letterbox 视频矩形 mask"
```

---

### Task 10: visual.css Aqua 玻璃卡片 + 交互样式

**Files:**
- Modify: `app/renderer/src/visual/visual.css`

**Interfaces:**
- Consumes: resolver 产出的 `--vs-glass-card-*` / `--vs-aqua-*` 令牌（Task 3）。
- Produces: `body[data-vtheme="aqua"]` 下的玻璃卡片配方、浮层映射、聚光/按压/边缘雾化/海洋生物样式。

- [ ] **Step 1: 追加 Aqua 样式块（文件末尾）**

```css
/* ============================================================
   Aqua — 深海水主题（body[data-vtheme="aqua"]）
   玻璃卡片配方 + 浮层映射 + 聚光/按压 + 边缘雾化 + 海洋生物
   ============================================================ */

/* 流体层：canvas 与壁纸（pointer-events none，位于氛围层内） */
.vs-fluid { position: absolute; inset: 0; pointer-events: none; }
.vs-fluid-canvas { position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0.85; }
.vs-fluid-wallpaper { position: absolute; inset: 0; overflow: hidden; }
.vs-fluid-wallpaper img {
  position: absolute; inset: 0; width: 100%; height: 100%;
  object-fit: cover;
  filter: blur(var(--vs-aqua-wallpaper-blur, 0px));
}
.vs-fluid-wallpaper::after {
  content: ""; position: absolute; inset: 0;
  background: rgb(255 255 255 / var(--vs-aqua-wallpaper-frost, 0));
}
body[data-theme="dark"] .vs-fluid-wallpaper::after { background: rgb(12 18 27 / var(--vs-aqua-wallpaper-frost, 0)); }

/* 亮度罩（brightness knob）：暗色压暗 / 浅色提亮 */
.vs-fluid::after {
  content: ""; position: absolute; inset: 0;
  background:
    linear-gradient(rgba(255,255,255,var(--vs-aqua-brightness-white,0)), rgba(255,255,255,var(--vs-aqua-brightness-white,0))),
    linear-gradient(rgba(0,0,0,var(--vs-aqua-brightness-black,0)), rgba(0,0,0,var(--vs-aqua-brightness-black,0)));
}

/* 海洋生物层 */
.vs-critters { position: absolute; inset: 0; pointer-events: none; overflow: hidden; }
.vs-whale {
  position: absolute; left: 50%; top: 52%;
  transform: translate(-50%, -50%);
  mix-blend-mode: screen; opacity: 0.9;
}
.vs-critter {
  position: absolute; color: #7ea4df; opacity: 0.22;
  font-size: 11px; line-height: 1; user-select: none;
}
.vs-critter[data-kind="fish"] { animation: vs-fish-swim 12s ease-in-out infinite; }
.vs-critter[data-kind="fish-left"] { animation: vs-fish-swim-left 16s ease-in-out infinite; }
.vs-critter[data-kind="bubble"] { color: #a9c6ef; opacity: 0; animation: vs-bubble-rise 9s ease-in infinite; font-size: 9px; }
.vs-critter[data-kind="plankton"] { animation: vs-plankton 5s ease-in-out infinite; font-size: 8px; }

@keyframes vs-fish-swim {
  0% { transform: translate3d(0,0,0) rotate(-5deg); }
  30% { transform: translate3d(40px,-15px,0) rotate(4deg); }
  70% { transform: translate3d(52px,-18px,0) rotate(3deg); }
  100% { transform: translate3d(0,0,0) rotate(-5deg); }
}
@keyframes vs-fish-swim-left {
  0% { transform: translate3d(0,0,0) scaleX(-1) rotate(-5deg); }
  30% { transform: translate3d(-34px,-12px,0) scaleX(-1) rotate(4deg); }
  70% { transform: translate3d(-44px,-15px,0) scaleX(-1) rotate(3deg); }
  100% { transform: translate3d(0,0,0) scaleX(-1) rotate(-5deg); }
}
@keyframes vs-bubble-rise {
  0% { transform: translate3d(0,0,0); opacity: 0; }
  10% { opacity: 0.5; }
  100% { transform: translate3d(8px,-150px,0); opacity: 0; }
}
@keyframes vs-plankton {
  0%, 100% { opacity: 0.1; }
  50% { opacity: 0.38; }
}

/* 边缘雾化带：悬浮滚动内容之上、玻璃卡之下 */
#vs-edge-top, #vs-edge-bottom {
  position: fixed; left: 0; right: 0; height: 13px;
  z-index: 7; pointer-events: none;
  backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(5px);
  background: rgba(0,0,0,0.15);
}
body[data-theme="light"] #vs-edge-top, body[data-theme="light"] #vs-edge-bottom { background: rgba(255,255,255,0.2); }
#vs-edge-top {
  top: 0;
  -webkit-mask-image: linear-gradient(180deg, black 0%, transparent 100%);
  mask-image: linear-gradient(180deg, black 0%, transparent 100%);
}
#vs-edge-bottom {
  bottom: 0;
  -webkit-mask-image: linear-gradient(0deg, black 0%, transparent 100%);
  mask-image: linear-gradient(0deg, black 0%, transparent 100%);
}

/* Aqua 玻璃卡片（浮层映射） */
body[data-vtheme="aqua"] .topbar,
body[data-vtheme="aqua"] .hero-card,
body[data-vtheme="aqua"] .tile,
body[data-vtheme="aqua"] .poster .cover,
body[data-vtheme="aqua"] .url-modal,
body[data-vtheme="aqua"] .nas-browser,
body[data-vtheme="aqua"] .control-deck,
body[data-vtheme="aqua"] .top-info,
body[data-vtheme="aqua"] .pop-menu,
body[data-vtheme="aqua"] .console-drawer,
body[data-vtheme="aqua"] .cast-toast,
body[data-vtheme="aqua"] .win-float,
body[data-vtheme="aqua"] .settings-topbar,
body[data-vtheme="aqua"] .settings-nav {
  background: var(--vs-glass-card-dark, rgba(22,25,34,0.5));
  border: 1px solid rgba(148,180,220,0.32);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.07),
    0 10px 36px rgba(2,6,14,0.5);
  backdrop-filter: blur(var(--vs-glass-blur, 20px)) saturate(1.15);
  -webkit-backdrop-filter: blur(var(--vs-glass-blur, 20px)) saturate(1.15);
}
body[data-theme="light"] body[data-vtheme="aqua"] .topbar,
body[data-theme="light"] body[data-vtheme="aqua"] .hero-card,
body[data-theme="light"] body[data-vtheme="aqua"] .tile,
body[data-theme="light"] body[data-vtheme="aqua"] .poster .cover,
body[data-theme="light"] body[data-vtheme="aqua"] .url-modal,
body[data-theme="light"] body[data-vtheme="aqua"] .nas-browser,
body[data-theme="light"] body[data-vtheme="aqua"] .control-deck,
body[data-theme="light"] body[data-vtheme="aqua"] .top-info,
body[data-theme="light"] body[data-vtheme="aqua"] .pop-menu,
body[data-theme="light"] body[data-vtheme="aqua"] .console-drawer,
body[data-theme="light"] body[data-vtheme="aqua"] .cast-toast,
body[data-theme="light"] body[data-vtheme="aqua"] .win-float,
body[data-theme="light"] body[data-vtheme="aqua"] .settings-topbar,
body[data-theme="light"] body[data-vtheme="aqua"] .settings-nav {
  background: var(--vs-glass-card-light, rgba(255,255,255,0.5));
  border-color: rgba(19,45,83,0.26);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.5),
    0 10px 36px rgba(19,45,83,0.16);
}

/* 大浮层 / 面板 / 原子圆角体系（20 / 14 / 8） */
body[data-vtheme="aqua"] .topbar,
body[data-vtheme="aqua"] .hero-card,
body[data-vtheme="aqua"] .control-deck,
body[data-vtheme="aqua"] .settings-nav { border-radius: 20px; }
body[data-vtheme="aqua"] .tile,
body[data-vtheme="aqua"] .poster .cover,
body[data-vtheme="aqua"] .pop-menu,
body[data-vtheme="aqua"] .console-drawer,
body[data-vtheme="aqua"] .url-modal,
body[data-vtheme="aqua"] .nas-browser { border-radius: 14px; }
body[data-vtheme="aqua"] .icon-btn { border-radius: 8px; }

/* 播放页特判：透明窗下玻璃=半透明染色（背后无 DOM 可模糊），保持可读 */
body[data-vtheme="aqua"] .control-deck,
body[data-vtheme="aqua"] .top-info {
  background: rgba(0,0,0,0.4);
  border-color: rgba(148,180,220,0.3);
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.07);
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
}

/* 光标聚光：注入的 glow div */
.vs-spot-glow {
  position: absolute; inset: 0;
  border-radius: inherit;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: -1;
}
[data-vs-spot] { position: relative; isolation: isolate; }
[data-vs-press] { transition: transform 0.1s ease-out; }

/* 滚动正文防糊（内容穿过玻璃边缘仍清晰） */
body[data-vtheme="aqua"] .page { text-shadow: 0 0 1px rgba(0,0,0,0.4); }
body[data-theme="light"] body[data-vtheme="aqua"] .page { text-shadow: 0 0 1px rgba(255,255,255,0.55), 0 1px 2px rgba(19,45,83,0.08); }

/* 焦点与选区（Aqua 蓝色系） */
body[data-vtheme="aqua"] :focus-visible { outline: 2px solid rgba(110,155,232,0.85); outline-offset: 1px; }
body[data-vtheme="aqua"] ::selection { background: rgba(110,155,232,0.35); }

/* reduced-motion：静默氛围运动 */
@media (prefers-reduced-motion: reduce) {
  .vs-critter, .vs-whale { animation: none !important; }
  [data-vs-press] { transition: none !important; }
}
```

- [ ] **Step 2: 验证类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过（CSS 改动不影响类型）。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/visual/visual.css
git commit -m "feat: Aqua 玻璃卡片配方/聚光按压/边缘雾化/海洋生物样式"
```

---

### Task 11: VisualConsole Aqua 页签 + 壁纸选择

**Files:**
- Modify: `app/renderer/src/visual/VisualConsole.tsx`

**Interfaces:**
- Consumes: `VisualStore.setParam`（支持 `aqua.*` 路径）、`useActiveTheme`、现有 tab 渲染管线。
- Produces: `Tab` 增加 `'aqua'`；`SLIDERS/SWITCHES/SEG_OPTS` 增加 aqua 组；仅当激活主题有 aqua 段时显示页签。

- [ ] **Step 1: 扩展类型与配置表**

```ts
type Tab = 'preset' | 'appearance' | 'atmosphere' | 'particles' | 'motion' | 'player' | 'advanced' | 'aqua';
```

```ts
const GROUPS: [Tab, string][] = [
  ['preset', 'Preset'], ['appearance', '外观'], ['atmosphere', '氛围'],
  ['particles', '粒子'], ['motion', '运动'], ['player', '播放器'], ['aqua', 'Aqua'], ['advanced', '高级'],
];
const DEFAULT_TABS: Tab[] = ['preset', 'appearance', 'atmosphere', 'particles', 'motion', 'player', 'aqua'];
```

在 `SLIDERS` 中追加 aqua 组：

```ts
  aqua: [
    { path: 'aqua.fluidHue', label: '流体色相', min: 0, max: 360, step: 1, fmt: (v) => `${Math.round(v)}°` },
    { path: 'aqua.fluidDepth', label: '流体深度', min: 0, max: 100, step: 1, fmt: (v) => `${Math.round(v)}` },
    { path: 'aqua.bgBrightness', label: '背景亮度', min: 0, max: 100, step: 1, fmt: (v) => `${Math.round(v)}` },
    { path: 'aqua.wallpaperBlur', label: '壁纸模糊', min: 0, max: 40, step: 1, fmt: (v) => `${Math.round(v)}px` },
    { path: 'aqua.wallpaperFrost', label: '壁纸磨砂', min: 0, max: 100, step: 1, fmt: (v) => `${Math.round(v)}%` },
  ],
```

在 `SWITCHES` 中追加 aqua 组：

```ts
  aqua: [
    ['aqua.edgeFade', '边缘雾化'],
    ['aqua.spotlight', '光标聚光'],
    ['aqua.press', '悬浮按压'],
    ['aqua.critters', '海洋生物'],
    ['aqua.whale', '粒子鲸鱼'],
  ],
```

在 `SEG_OPTS` 中追加背景源：

```ts
  'aqua.backdrop': [['fluid', '流体'], ['wallpaper', '壁纸']],
```

- [ ] **Step 2: 页签可见性 —— 仅当激活主题有 aqua 段**

修改页签渲染（`GROUPS.filter(...)`）：

```ts
          {GROUPS.filter(([id]) => id === 'advanced' || DEFAULT_TABS.includes(id)).filter(([id]) => id !== 'aqua' || VisualStore.getActiveTheme().aqua).map(([id, l]) => (
```

- [ ] **Step 3: aqua 页签追加壁纸文件选择行**

在 `paramsPane()` 中，`if (tab === 'advanced')` 之前插入：

```ts
    if (tab === 'aqua') {
      rows.push(segRow('aqua.backdrop', '背景源'));
      rows.push(
        <div className="vc-row" key="wallpaper">
          <span>壁纸</span>
          <div className="vc-seg">
            <label className="vc-file" style={{ cursor: 'pointer' }}>
              选择图片
              <input type="file" accept="image/*" style={{ display: 'none' }}
                onChange={(e) => {
                  const f = e.target.files?.[0];
                  if (!f) return;
                  const fr = new FileReader();
                  fr.onload = () => { VisualStore.setParam('aqua.wallpaper', String(fr.result)); toast('已设置壁纸'); };
                  fr.readAsDataURL(f);
                  e.target.value = '';
                }} />
            </label>
            <button onClick={() => { VisualStore.setParam('aqua.wallpaper', ''); toast('已清除壁纸'); }}>清除</button>
          </div>
        </div>,
      );
    }
```

- [ ] **Step 4: 类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 5: 提交**

```bash
git add app/renderer/src/visual/VisualConsole.tsx
git commit -m "feat: VisualConsole Aqua 页签（流体/壁纸/亮度/生物开关，零 IPC 壁纸选择）"
```

---

### Task 12: 播放页玻璃染色特判（styles.css 兜底）

**Files:**
- Modify: `app/renderer/src/styles.css`

**Interfaces:**
- Consumes: `body[data-vtheme="aqua"]` 选择器。
- Produces: 播放页 `control-deck`/`top-info` 在 Aqua 下保持半透明染色且不被视觉系统变量覆盖时仍可读。

- [ ] **Step 1: 在 `styles.css` 的 `.glass-1` 附近追加兜底（覆盖 visual.css 中的 backdrop-filter:none 特判若被旧选择器抢占）**

在 `/* ---------- Glass 三级（仅浮层） ---------- */` 块后追加：

```css
/* Aqua：播放页玻璃控件保持半透明染色（透明窗下 backdrop-filter 无效），优先级高于 visual.css 泛化规则 */
body.player[data-vtheme="aqua"] .control-deck,
body.player[data-vtheme="aqua"] .top-info {
  background: rgba(0, 0, 0, .42);
  border: 1px solid rgba(148, 180, 220, .3);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, .07);
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
}
body[data-theme="light"].player[data-vtheme="aqua"] .control-deck,
body[data-theme="light"].player[data-vtheme="aqua"] .top-info {
  background: rgba(255, 255, 255, .5);
  border-color: rgba(19, 45, 83, .22);
}
```

- [ ] **Step 2: 类型 + 构建**

Run: `npx tsc --noEmit`；`npm run build`
Expected: 零错误，build 通过。

- [ ] **Step 3: 提交**

```bash
git add app/renderer/src/styles.css
git commit -m "feat: 播放页 Aqua 玻璃染色兜底（透明窗半透明染色）"
```

---

### Task 13: 文档更新 + 全量回归

**Files:**
- Modify: `docs/06-视觉主题系统设计.md`

- [ ] **Step 1: 追加 Aqua 主题设计节**

在 `docs/06-视觉主题系统设计.md` 末尾追加：

```markdown
## 17. Aqua 主题（第 7 内置，默认）

**来源：** 仿照 DSH-Transparent-UI-Plugin（Aqua 毛玻璃主题）。完整设计见 `docs/superpowers/specs/2026-08-17-aqua-ui-design.md`。

**特性：** 深海军蓝/冷白双外观 · 悬浮毛玻璃卡片（20/14/8 圆角体系）· WebGL 流体背景（色相/深度可调）+ 自定义壁纸 · 顶/底边缘雾化 · 光标聚光 + hover 3D 按压 · 粒子鲸鱼 + 鱼/气泡/浮游海洋生物。

**架构：** Aqua 为第 7 内置主题（`registry.ts`），`theme.aqua` 可选段承载专属参数；新增 4 个运行时单元（FluidEngine / CrittersEngine / SpotlightEngine / EdgeFadeLayer），controller 统一编排。播放页受透明窗约束：流体 mask 到 letterbox 边缘带，中心露出 mpv；视频充满窗口时流体隐藏。

**默认值：** `store.ts` 默认 `activeThemeId='aqua'`；新装/迁移无映射落 aqua；已有 `visual.json` 用户保持当前主题。编辑官方 Aqua 自动 clone 为 Custom Copy。
```

- [ ] **Step 2: 全量回归**

Run（在 `app/` 下）：
```bash
npx tsc --noEmit
npm run build
node scripts/visual-test.js
node scripts/hdr-test.js
node scripts/dmc-test.js
node scripts/pipe-test.js
node scripts/security-test.js
```
Expected: 全绿（visual-test `RESULT ok=... fail=0`；hdr 23/23；dmc 21/21；pipe PIPE OK；security 12/12）。

- [ ] **Step 3: 真机 QA 清单（手动，需 mpv 运行时 + 显示器）**

```text
1. 启动 → 首页默认 Aqua：深海军蓝底、流体背景可见、顶部 topbar 毛玻璃、边缘雾化带存在。
2. 明暗切换：Aqua dark 深蓝 / Aqua light 冷白蓝，字体对比可读。
3. 播放 16:9 与 2.35:1 视频：letterbox 边缘带出现流体、中心视频区域无覆盖；全屏同宽高比视频流体隐藏。
4. 鼠标划过 topbar/tile/control-deck：光标聚光跟随；hover 时卡片微倾斜（reduced-motion 关闭时）。
5. 打开 Visual Console → Aqua 页签：调色相/深度/背景亮度实时生效；切壁纸源选图生效；切回流体恢复。
6. 海洋生物：鲸鱼仅首页/设置页居中显示；播放页隐藏鲸鱼、critters 降透明。
7. 切换 Cinema/Noir 等主题：Aqua 页签消失、流体/鲸鱼/聚光停止，无残留。
8. 黑场中心 lum ≤0.067、白场中心 ≥0.94 复测；drop-frame Δ=0。
9. 截图存入 prototype/redesign/qa-shots/（Home+Aqua 暗/浅、Player+Aqua letterbox、Console+Aqua、wallpaper）。
```

- [ ] **Step 4: 提交**

```bash
git add docs/06-视觉主题系统设计.md
git commit -m "docs: 记录 Aqua 第 7 主题设计（默认外观，仿 DSH-Transparent-UI-Plugin）"
```

---

## Self-Review 记录

- **Spec 覆盖**：spec §3（schema/引擎编排）→ Task 1/5-9；§4（玻璃卡片/页面浮层/交互）→ Task 3/10/12；§5（Aqua 参数）→ Task 2；§6（默认/迁移）→ Task 4；§7（Console）→ Task 11；§8（测试）→ Task 13；§9（文件清单）→ 全部任务。
- **占位符**：无 TBD/TODO；每步含完整代码。
- **类型一致性**：`AquaEngineParams` 在 Task 1 定义，Task 3 resolver 产出、Task 5-8 引擎消费、Task 9 controller 接线，命名一致；`setVideoRect` 的 rect 形状在 Task 5 定义、Task 9 传入一致；`vs-aqua-*` CSS 变量在 Task 3 产出、Task 10 消费一致。
- **注意点**：Task 3 `sceneOf` 的 fluid 分支需在 `mode==='cover'` 判断之前插入（`fluid`/`wallpaper` 与 `cover`/`gradient` 互斥）；Task 2 的测试计数改动必须与 registry 改动同批提交，否则中间态测试红。
